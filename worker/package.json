{
  "name": "zoho-forms-backup-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "d1:migrate": "wrangler d1 migrations apply zoho-backup --remote"
  },
  "devDependencies": {
    "wrangler": "^4.103.0",
    "@cloudflare/workers-types": "latest",
    "typescript": "latest"
  }
}
